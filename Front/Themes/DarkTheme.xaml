<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vmbase="clr-namespace:vcv_etagere_remaster.Front.ViewModel.Base;assembly=vcv_etagere_remaster">

    <!-- Clean Dark ComboBox ToggleButton Template -->
    <ControlTemplate x:Key="ComboBoxToggleButtonTemplate" TargetType="ToggleButton">
        <Border x:Name="templateRoot" 
                Background="#2a2a2d" 
                BorderBrush="#444" 
                BorderThickness="1" 
                CornerRadius="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                </Grid.ColumnDefinitions>
                <Border x:Name="splitBorder" 
                        Grid.Column="1" 
                        BorderBrush="Transparent" 
                        BorderThickness="0" 
                        HorizontalAlignment="Right" 
                        Width="24">
                    <Path x:Name="arrow" 
                          Data="M 0 0 L 4 4 L 8 0 Z" 
                          Fill="#aaa" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter TargetName="templateRoot" Property="Background" Value="#3a3a3d"/>
                <Setter TargetName="arrow" Property="Fill" Value="White"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="true">
                <Setter TargetName="templateRoot" Property="Background" Value="#1e1e1f"/>
                <Setter TargetName="arrow" Property="Fill" Value="White"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Clean Dark ComboBox Template -->
    <ControlTemplate x:Key="ComboBoxTemplate" TargetType="ComboBox">
        <Grid x:Name="templateRoot">
            <ToggleButton x:Name="toggleButton" 
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                          Template="{StaticResource ComboBoxToggleButtonTemplate}"
                          Focusable="false" 
                          ClickMode="Press"/>
            
            <ContentPresenter x:Name="contentPresenter" 
                              Content="{TemplateBinding SelectionBoxItem}" 
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" 
                              HorizontalAlignment="Left" 
                              VerticalAlignment="Center" 
                              Margin="8,0,24,0" 
                              IsHitTestVisible="false"/>
            
            <Popup x:Name="shadowMaskPopup" 
                   AllowsTransparency="true" 
                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                   Placement="Bottom" 
                   PopupAnimation="Slide">
                <Border x:Name="dropDownBorder" 
                        Background="#1e1e1e" 
                        BorderBrush="#444" 
                        BorderThickness="1" 
                        CornerRadius="3"
                        MinHeight="20"
                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                    <ScrollViewer x:Name="dropDownScrollViewer" SnapsToDevicePixels="True">
                        <ItemsPresenter x:Name="itemsPresenter" 
                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
    </ControlTemplate>

    <!-- Global ComboBoxItem Style -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="Transparent" 
                            BorderThickness="0" 
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background" Value="#3e3e42"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="border" Property="Background" Value="#5c1313"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Global ComboBox Style -->
    <Style TargetType="ComboBox">
        <Setter Property="Template" Value="{StaticResource ComboBoxTemplate}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#2a2a2d"/>
        <Setter Property="BorderBrush" Value="#444"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <!-- Global Menu Style -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="#1c1c1f"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#111"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <!-- Global ContextMenu Style -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="#1e1e1e"/>
        <Setter Property="BorderBrush" Value="#444"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="0,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="3"
                            SnapsToDevicePixels="true">
                        <ScrollViewer CanContentScroll="true" Margin="0,2"
                                      VerticalScrollBarVisibility="Disabled"
                                      HorizontalScrollBarVisibility="Disabled">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Clean Dark MenuItem Template -->
    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="templateRoot" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            SnapsToDevicePixels="true">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemGestureColumnGroup"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Header Column -->
                            <ContentPresenter x:Name="contentPresenter" 
                                               Grid.Column="0" 
                                               ContentSource="Header" 
                                               RecognizesAccessKey="true" 
                                               VerticalAlignment="Center"
                                               Margin="10,2,15,2"/>
                            
                            <!-- Gesture Column -->
                            <TextBlock x:Name="InputGestureText" 
                                       Grid.Column="1" 
                                       Text="{TemplateBinding InputGestureText}" 
                                       Foreground="#888" 
                                       VerticalAlignment="Center" 
                                       Margin="5,2"/>
                            
                            <!-- Submenu Indicator -->
                            <Path x:Name="RightArrow" 
                                  Grid.Column="2" 
                                  Data="M 0 0 L 4 4 L 0 8 Z" 
                                  Fill="#888" 
                                  VerticalAlignment="Center" 
                                  Margin="10,0,5,0" 
                                  Visibility="Collapsed"/>
                            
                            <!-- Submenu Popup -->
                            <Popup x:Name="INT_Popup" 
                                   AllowsTransparency="true" 
                                   Focusable="false" 
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                   Placement="Right" 
                                   PopupAnimation="Slide">
                                <Border x:Name="SubmenuBorder" 
                                        Background="#1c1c1f" 
                                        BorderBrush="#444" 
                                        BorderThickness="1" 
                                        CornerRadius="3"
                                        Padding="0,2">
                                    <ScrollViewer x:Name="SubmenuScrollViewer" CanContentScroll="true"
                                                  VerticalScrollBarVisibility="Disabled"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Hover states -->
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="templateRoot" Property="Background" Value="#3e3e42"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="true">
                            <Setter TargetName="templateRoot" Property="Background" Value="#3e3e42"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="INT_Popup" Property="Placement" Value="Bottom"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="INT_Popup" Property="Placement" Value="Right"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Collapsed"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="#555555"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Global Separator Style -->
    <Style TargetType="Separator">
        <Setter Property="Background" Value="#333"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="0,2"/>
    </Style>

    <!-- Global ToolTip Style -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="#252526"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="#434346"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="HasDropShadow" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            SnapsToDevicePixels="true">
                        <ContentPresenter Margin="{TemplateBinding Padding}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Implicit DataTemplate for PortViewModelBase (Custom Styled Port Hover Display) -->
    <DataTemplate DataType="{x:Type vmbase:PortViewModelBase}">
        <StackPanel Margin="2">
            <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#ffd080" FontSize="11" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding TypeDisplay}" Foreground="White" FontSize="10" Margin="0,0,0,2"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                <TextBlock Text="Statut : " Foreground="#aaa" FontSize="10"/>
                <TextBlock Text="{Binding StatusDisplay}" Foreground="{Binding ConnectionColor}" FontSize="10" FontWeight="Bold"/>
            </StackPanel>
            <TextBlock Text="{Binding ValueDisplay, StringFormat='Tension : {0}'}" Foreground="#85d7ff" FontSize="10"/>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>
