<UserControl x:Class="vcv_etagere_remaster.Front.View.Modules.ReverbView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="150"
             Width="160" Height="360" Margin="10"
             Background="#2e2e2e" BorderBrush="#1a1a1a" BorderThickness="2">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Module Header -->
        <Border Grid.Row="0" Background="#2e113d" CornerRadius="3" Padding="5">
            <TextBlock Text="{Binding Name}" Foreground="#c397e8" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <!-- Reverb Parameters -->
        <StackPanel Grid.Row="1" Margin="5,15,5,0">
            <!-- Active Toggle Button -->
            <CheckBox IsChecked="{Binding IsActive}" Content="ACTIVE" Foreground="White" FontSize="10" HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <!-- Mix Slider -->
            <TextBlock Text="MIX" Foreground="Gray" FontSize="9" HorizontalAlignment="Center"/>
            <Slider Minimum="0.0" Maximum="1.0" Value="{Binding Mix}" Orientation="Horizontal" Margin="0,2,0,8"/>

            <!-- Decay Slider -->
            <TextBlock Text="DECAY (ROOM SIZE)" Foreground="Gray" FontSize="9" HorizontalAlignment="Center"/>
            <Slider Minimum="0.0" Maximum="0.95" Value="{Binding RoomSize}" Orientation="Horizontal" Margin="0,2,0,8"/>

            <!-- Damp Slider -->
            <TextBlock Text="DAMPING" Foreground="Gray" FontSize="9" HorizontalAlignment="Center"/>
            <Slider Minimum="0.0" Maximum="0.5" Value="{Binding Damp}" Orientation="Horizontal" Margin="0,2,0,5"/>
        </StackPanel>

        <!-- Input Ports (Stereo) -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding InputPorts}" Margin="0,5">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5,2">
                        <TextBlock Text="{Binding Name}" Foreground="LightGray" FontSize="8" HorizontalAlignment="Center"/>
                        <Ellipse Width="20" Height="20" Fill="#111" Stroke="Gray" StrokeThickness="2" Margin="0,1"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Output Ports (Stereo) -->
        <ItemsControl Grid.Row="3" ItemsSource="{Binding OutputPorts}" Margin="0,5">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5,2">
                        <TextBlock Text="{Binding Name}" Foreground="LightGray" FontSize="8" HorizontalAlignment="Center"/>
                        <Ellipse Width="20" Height="20" Fill="#111" Stroke="White" StrokeThickness="2" Margin="0,1"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
