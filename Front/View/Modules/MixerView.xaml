<UserControl x:Class="vcv_etagere_remaster.Front.View.Modules.MixerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="160"
             Width="160" Height="400"
             Background="#222225" BorderBrush="#161618" BorderThickness="2">

    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Module Header — DragHandle -->
            <Border Grid.Row="0" Background="#5c1313" CornerRadius="3" Padding="5,5,28,5"
                    Tag="DragHandle" Cursor="SizeAll">
                <TextBlock Text="{Binding Name}" Foreground="#ff8a8a" FontWeight="Bold"
                           HorizontalAlignment="Center" IsHitTestVisible="False"/>
            </Border>

            <!-- Parameters Grid -->
            <StackPanel Grid.Row="1" Margin="5,15,5,0">
                
                <!-- Channel 1 Controls -->
                <StackPanel Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="CHANNEL 1" Foreground="#ff8a8a" FontSize="9" FontWeight="Bold" VerticalAlignment="Center"/>
                        
                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsMuted1}" Cursor="Hand">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Border" Background="#2a2a2d" CornerRadius="3" Padding="5,2" BorderBrush="#444" BorderThickness="1">
                                        <TextBlock x:Name="Text" Text="MUTE" Foreground="#888" FontSize="8" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#aa2222"/>
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#ff5555"/>
                                            <Setter TargetName="Text" Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#666"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                    </Grid>
                    
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="VOL" Foreground="Gray" FontSize="8" VerticalAlignment="Center"/>
                        <Slider Grid.Column="1" Minimum="0.0" Maximum="1.5" Value="{Binding Level1}" Orientation="Horizontal"/>
                    </Grid>
                    
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="PAN" Foreground="Gray" FontSize="8" VerticalAlignment="Center"/>
                        <Slider Grid.Column="1" Minimum="-1.0" Maximum="1.0" Value="{Binding Pan1}" Orientation="Horizontal"/>
                    </Grid>
                </StackPanel>

                <!-- Channel 2 Controls -->
                <StackPanel Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="CHANNEL 2" Foreground="#ff8a8a" FontSize="9" FontWeight="Bold" VerticalAlignment="Center"/>
                        
                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsMuted2}" Cursor="Hand">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Border" Background="#2a2a2d" CornerRadius="3" Padding="5,2" BorderBrush="#444" BorderThickness="1">
                                        <TextBlock x:Name="Text" Text="MUTE" Foreground="#888" FontSize="8" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#aa2222"/>
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#ff5555"/>
                                            <Setter TargetName="Text" Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="BorderBrush" Value="#666"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                    </Grid>
                    
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="VOL" Foreground="Gray" FontSize="8" VerticalAlignment="Center"/>
                        <Slider Grid.Column="1" Minimum="0.0" Maximum="1.5" Value="{Binding Level2}" Orientation="Horizontal"/>
                    </Grid>
                    
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="PAN" Foreground="Gray" FontSize="8" VerticalAlignment="Center"/>
                        <Slider Grid.Column="1" Minimum="-1.0" Maximum="1.0" Value="{Binding Pan2}" Orientation="Horizontal"/>
                    </Grid>
                </StackPanel>

                <!-- Master Controls -->
                <StackPanel Margin="0,5,0,0">
                    <TextBlock Text="MASTER" Foreground="#ff8a8a" FontSize="9" FontWeight="Bold" Margin="0,0,0,2"/>
                    <Grid Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="VOL" Foreground="Gray" FontSize="8" VerticalAlignment="Center"/>
                        <Slider Grid.Column="1" Minimum="0.0" Maximum="1.5" Value="{Binding MasterLevel}" Orientation="Horizontal"/>
                    </Grid>
                </StackPanel>
            </StackPanel>

            <!-- Input Ports -->
            <ItemsControl Grid.Row="2" ItemsSource="{Binding InputPorts}" Margin="0,5">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Margin="8,2" Background="Transparent" ToolTip="{Binding Name}">
                            <TextBlock Text="{Binding Name}" Foreground="LightGray" FontSize="8" HorizontalAlignment="Center"/>
                            <Ellipse Width="20" Height="20" Fill="#111" Stroke="Gray" StrokeThickness="2" Margin="0,1"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Output Ports -->
            <ItemsControl Grid.Row="3" ItemsSource="{Binding OutputPorts}" Margin="0,5">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Margin="8,2" Background="Transparent" ToolTip="{Binding Name}">
                            <TextBlock Text="{Binding Name}" Foreground="LightGray" FontSize="8" HorizontalAlignment="Center"/>
                            <Ellipse Width="20" Height="20" Fill="#111" Stroke="White" StrokeThickness="2" Margin="0,1"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <!-- Delete button -->
        <Button Command="{Binding RemoveCommand}"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Width="22" Height="22" Margin="0,6,6,0"
                Cursor="Hand" ToolTip="Supprimer le module"
                Panel.ZIndex="10">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="#aa2222" CornerRadius="11" Width="22" Height="22">
                        <TextBlock Text="✕" Foreground="White" FontSize="11" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#ff3333"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
