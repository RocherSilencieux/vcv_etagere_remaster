<UserControl x:Class="vcv_etagere_remaster.Front.View.Modules.AudioOutputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="150"
             Width="160" Height="360" Margin="10"
             Background="#2e2e2e" BorderBrush="#1a1a1a" BorderThickness="2">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Module Header -->
        <Border Grid.Row="0" Background="#3a1a1a" CornerRadius="3" Padding="5">
            <TextBlock Text="{Binding Name}" Foreground="#ff4444" FontWeight="Bold" HorizontalAlignment="Center"/>
        </Border>

        <!-- Master Volume Control -->
        <StackPanel Grid.Row="1" Margin="0,15,0,0">
            <TextBlock Text="MASTER VOL" Foreground="Gray" HorizontalAlignment="Center" FontSize="10"/>
            <!-- Slider for Master Volume from 0.0 to 1.0 -->
            <Slider Minimum="0.0" Maximum="1.0" Value="{Binding MasterVolume}" Orientation="Vertical" Height="90" HorizontalAlignment="Center" Margin="0,5,0,15"/>
        </StackPanel>

        <!-- Audio Inputs -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding InputPorts}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5,2">
                        <TextBlock Text="{Binding Name}" Foreground="LightGray" FontSize="10" HorizontalAlignment="Center"/>
                        <Ellipse Width="25" Height="25" Fill="#111" Stroke="#ff4444" StrokeThickness="2" Margin="0,2"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Audio Device Selection -->
        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock Text="DEVICE" Foreground="Gray" HorizontalAlignment="Center" FontSize="10" Margin="0,0,0,3"/>
            <ComboBox ItemsSource="{Binding AvailableDevices}"
                      SelectedItem="{Binding SelectedDevice}"
                      DisplayMemberPath="Name"
                      Background="#1a1a1a"
                      Foreground="White"
                      BorderBrush="#444"
                      FontSize="10"
                      HorizontalContentAlignment="Stretch">
                <ComboBox.ItemContainerStyle>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="Black"/>
                    </Style>
                </ComboBox.ItemContainerStyle>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>

